{% extends 'base.html.twig' %}

{% block title %}Nova Unidade{% endblock %}

{% block body %}
 <div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">Nova Unidade</h6>
    </div>
    <div class="card-body">
        {{ form_start(form, {'class':'form-group', 'attr': { 'id':'unidade_form'}}) }}
        {% form_theme form with 'bootstrap_4_layout.html.twig' %}
            {{ include('unidade/_form.html.twig') }}
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}