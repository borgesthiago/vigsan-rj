<form method="post" action="{{ path('unidade_delete_admin', {'id': unidade.id}) }}" onsubmit="return confirm('Deseja realmente excluir?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unidade.id) }}">
    <button class="btn btn-outline-danger btn-sm float-right mb-2"><i class="fa fa-trash"></i> Delete</button>
</form>
