{% extends 'admin/base.html.twig' %}

{% block title %}Edit Comorbidade{% endblock %}

{% block body %}
 <div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">Editar Comorbidade</h6>
    </div>
    <div class="card-body">
        {{ form_start(form, {'class':'form-group', 'attr': { 'id':'comorbidade_form'}}) }}
        {% form_theme form with 'bootstrap_4_layout.html.twig' %}
            {{ include('admin/comorbidade/_form.html.twig') }}
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
